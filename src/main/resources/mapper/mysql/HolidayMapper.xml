<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.HolidayMapper">

	<!-- 祝日一覧検索 -->
	<select id="findMany"
		resultType="com.example.demo.domain.model.Holiday">

		SELECT
			id,
			yyyymmdd,
			holiday_name
		FROM
			holidays

		<where>
			<if test="yyyymmdd != null and yyyymmdd != ''">
				AND yyyymmdd = #{yyyymmdd}
			</if>

			<if test="holidayName != null and holidayName != ''">
				AND holiday_name LIKE CONCAT('%', #{holidayName}, '%')
			</if>
		</where>

		ORDER BY
			yyyymmdd

		LIMIT
			#{pageSize}
		OFFSET
			#{offset}

	</select>

	<!-- 検索結果件数取得 -->
	<select id="count"
		resultType="int">

		SELECT
			COUNT(*)
		FROM
			holidays

		<where>
			<if test="yyyymmdd != null and yyyymmdd != ''">
				AND yyyymmdd = #{yyyymmdd}
			</if>

			<if test="holidayName != null and holidayName != ''">
				AND holiday_name LIKE CONCAT('%', #{holidayName}, '%')
			</if>
		</where>

	</select>

	<!-- 指定期間の祝日一覧取得 -->
	<select id="findByPeriod"
		resultType="com.example.demo.domain.model.Holiday">

		SELECT
			id,
			yyyymmdd,
			holiday_name
		FROM
			holidays
		WHERE
			yyyymmdd BETWEEN #{startDate} AND #{endDate}
		ORDER BY
			yyyymmdd

	</select>

	<!-- IDで1件取得 -->
	<select id="findOne"
		resultType="com.example.demo.domain.model.Holiday">

		SELECT
			id,
			yyyymmdd,
			holiday_name
		FROM
			holidays
		WHERE
			id = #{id}

	</select>

	<!-- 祝日登録 -->
	<insert id="insert">

		INSERT INTO holidays (
			yyyymmdd,
			holiday_name
		)
		VALUES (
			#{yyyymmdd},
			#{holidayName}
		)

	</insert>

	<!-- 祝日更新 -->
	<update id="update">

		UPDATE
			holidays
		SET
			yyyymmdd = #{yyyymmdd},
			holiday_name = #{holidayName}
		WHERE
			id = #{id}

	</update>

	<!-- 祝日削除 -->
	<delete id="delete">

		DELETE FROM
			holidays
		WHERE
			id = #{id}

	</delete>

</mapper>